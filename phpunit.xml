<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
    </testsuites>
    <coverage processUncoveredFiles="true">
        <include>
            <directory suffix=".php">./app</directory>
        </include>
    </coverage>
    <php>
        <!--
            🔴 1536M y no 512M: la misma enfermedad de fondo (misión 60, 14/8/2026: "sin esto la
            suite MUERE a mitad y el gate del carril de merge no puede medir nada") volvió a
            aparecer. 512M alcanzaba con 117 tests; medido el 9/9/2026 con la suite ya en 1436
            tests, la corrida completa vuelve a morir por memoria — ahora en
            RobustezDelDeploymentDesatendidoTest (que acumula el contenido de TODO app/ en un
            string, por reflexión, en más de un método de test) con "Allowed memory size of
            536870912 bytes exhausted". Mismo síntoma que el de 2026: un fatal por memoria mata el
            proceso de PHP entero, phpunit no llega a escribir el XML de junit, y el carril aborta
            con "no se pudo medir" en vez de con rojos.

            No es ninguna aserción tocada ni un cambio de comportamiento: es el mismo techo de
            siempre, corrido hacia arriba porque la suite creció. Sube el techo de la corrida de
            tests y nada más: no toca el php.ini de Apache ni el límite con el que corre la
            aplicación en producción.
        -->
        <ini name="memory_limit" value="1536M"/>
        <server name="APP_ENV" value="testing"/>
        <server name="BCRYPT_ROUNDS" value="4"/>
        <server name="CACHE_DRIVER" value="array"/>
        <!-- <server name="DB_CONNECTION" value="sqlite"/> -->
        <!-- <server name="DB_DATABASE" value=":memory:"/> -->
        <server name="MAIL_MAILER" value="array"/>
        <server name="QUEUE_CONNECTION" value="sync"/>
        <server name="SESSION_DRIVER" value="array"/>
        <server name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
